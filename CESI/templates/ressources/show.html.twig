

{% block title %}Ressources{% endblock %}

{% block body %}
    <h1>Ressources</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ressource.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ ressource.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ressource.description }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ ressource.media }}</td>
            </tr>
            <tr>
                <th>Private</th>
                <td>{{ ressource.private ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ressources_index') }}">back to list</a>

    <a href="{{ path('ressources_edit', {'id': ressource.id}) }}">edit</a>

    {{ include('ressources/_delete_form.html.twig') }}
{% endblock %}
