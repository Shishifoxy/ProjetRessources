<form method="post" action="{{ path('types_ressources_delete', {'id': types_ressource.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_ressource.id) }}">
    <button class="btn">Delete</button>
</form>
