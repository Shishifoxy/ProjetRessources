

{% block title %}TypesRessources{% endblock %}

{% block body %}
    <h1>TypesRessources</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_ressource.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ types_ressource.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('types_ressources_index') }}">back to list</a>

    <a href="{{ path('types_ressources_edit', {'id': types_ressource.id}) }}">edit</a>

    {{ include('types_ressources/_delete_form.html.twig') }}
{% endblock %}
